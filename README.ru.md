# EclipseLink Analyzer 🔍

**EclipseLink Analyzer** — это инструмент для расширенной визуализации и статического анализа проектов JPA/EclipseLink. Он извлекает метамодель персистентности для создания интерактивной карты ваших сущностей, автоматически обнаруживая аномалии маппинга, риски производительности и архитектурные проблемы.

## 🚀 Ключевые Возможности

### 1. **Интерактивный Граф Домена**
*   **Визуализация связей**: One-to-One, One-to-Many, Many-to-Many, Наследование.
*   **Слои DDD**: Автоматическая группировка по Агрегатам или Ограниченным Контекстам (Bounded Contexts).
*   **Виртуальные Узлы**: Отичительная визуализация для `MappedSuperclass` и `Embeddable` (пунктирные границы).

### 2. **Статический Анализ и Диагностика**
*   **⚠️ Аномалии Схемы**: Обнаруживает сломанные маппинги, отсутствующие ID или неправильные настройки каскадирования.
*   **⚡ Риски Производительности**: Выявляет `EAGER` загрузку, проблему N+1 и слишком большие коллекции.
*   **🔄 Обнаружение Циклов**:
    *   **Красные Циклы**: Подсвечивает сложные циклические зависимости (A -> B -> C -> A) **КРАСНЫМ**.
    *   **Двунаправленные**: Отличает простые двунаправленные связи (A <-> B) оранжевым цветом.

### 3. **Эргономичные Режимы Просмотра**
Переключайтесь между различными перспективами через единое выпадающее меню:
*   **Обзор (Overview)**: Полный граф.
*   **Bounded Contexts**: Группировка сущностей по пакетам с визуальными рамками.
*   **Aggregates**: Фокус на корневых сущностях (Aggregate Roots).

## 🛠️ Установка и Использование

### 1. Бэкенд (Java)
Соберите анализатор для генерации JSON-отчета из ваших сущностей.

```bash
cd analyzer-backend
mvn clean install
# Запуск анализатора
mvn exec:java -Dexec.mainClass="com.eclipselink.analyzer.Main"
```

### 2. Фронтенд (React)
Визуализируйте сгенерированный отчет.

```bash
cd analyzer-frontend
npm install
npm run dev
```
Откройте `http://localhost:5173` и загрузите ваш `.json` отчет или выберите демо-сценарий.
